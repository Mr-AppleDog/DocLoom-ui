apiVersion: v1
kind: Service
metadata:
  name: docloom-ui
spec:
  # NodePort 暴露:避开 CICD 在 traefik /(:80)上的 ingress 冲突
  # 访问:http://192.168.149.128:30080
  type: NodePort
  selector:
    app: docloom-ui
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
